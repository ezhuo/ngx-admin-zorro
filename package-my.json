{
    "scripts": {
        "start": "ng serve --port 4201 --proxy-config proxy.conf.json",
        "build:bh": "ng build --prod --build-optimizer --bh /dist/",
    },
    "dependencies": {
        "blueimp-md5": "^2.10.0",
        "angular2-jwt": "^0.2.3",
        "jquery": "^3.2.1",
        "jstree": "^3.3.5"
    }
}
